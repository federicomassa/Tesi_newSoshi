#ifndef GAUS_FITTER_H
#define GAUS_FITTER_H

class TF1;
class TH1F;

class GaussFitter {
  
  unsigned int m_iterationNumber;
  TF1* m_fitFunc;
  const TH1F* const m_fitHist;
 public:
  GaussFitter();
  TF1* Fit(const TH1F* const);




};

#endif //GAUS_FITTER_H
